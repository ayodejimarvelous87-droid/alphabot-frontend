export default function AlphaLogo({ className = "" }) {
  return (
    <img
      src="/alphabot-logo.jpg"
      alt="AlphaBot — Powering Possibilities"
      className={`object-contain ${className}`}
    />
  );
}
